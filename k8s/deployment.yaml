apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentic-email-processing-system
  namespace: statex-apps
  labels:
    app: agentic-email-processing-system
    managed-by: k8s-migration
spec:
  replicas: 1
  selector:
    matchLabels:
      app: agentic-email-processing-system
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  template:
    metadata:
      labels:
        app: agentic-email-processing-system
    spec:
      containers:
        - name: app
          image: localhost:5000/agentic-email-processing-system:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3374
              name: http
          envFrom:
            - configMapRef:
                name: agentic-email-processing-system-config
            - secretRef:
                name: agentic-email-processing-system-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 3374
            initialDelaySeconds: 15
            periodSeconds: 30
            failureThreshold: 3
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: 3374
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
            timeoutSeconds: 5
          resources:
            requests:
              memory: "128Mi"
              cpu: "50m"
            limits:
              memory: "512Mi"
              cpu: "500m"

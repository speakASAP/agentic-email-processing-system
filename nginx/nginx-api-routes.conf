# API Routes Configuration
# Picked up by nginx-microservice deploy-smart.sh for blue/green (container -blue/-green, nginx-network).
# Routes are routed to the backend (agentic-email-processing-system).

/api/ingest
/api/classify
/api/extract
/api/decide
/api/triage
/health

# Agentic Email Processing System — keys only (no secret values)
# See CREATE_SERVICE.md and README.md. Copy to .env and set values.

# =============================================================================
# Service identity
# =============================================================================
NODE_ENV
DOMAIN
SERVICE_NAME
NGINX_NETWORK_NAME

# =============================================================================
# Local logs (applications directory — logs stored in 3 places: central service, in-memory, and this dir)
# =============================================================================
LOG_DIR

# =============================================================================
# Port configuration
# Allowed range: 33xx. 3371-3373=auth (root README). This service: 3374 (blue), 3375 (green).
# =============================================================================
PORT
PORT_BLUE
PORT_GREEN

# =============================================================================
# Shared microservices (same variable names as other services in the ecosystem)
# =============================================================================
AUTH_SERVICE_URL
LOGGING_SERVICE_URL
LOGGING_SERVICE_API_PATH
AI_SERVICE_URL
AI_INGEST_TIMEOUT_MS
NOTIFICATION_SERVICE_URL

# =============================================================================
# Database (optional)
# =============================================================================
DB_HOST
DB_PORT
DB_USER
DB_PASSWORD
DB_NAME

# =============================================================================
# Email ingestion
# =============================================================================
EMAIL_INGESTION_URL
EMAIL_INGESTION_QUEUE

# =============================================================================
# Demo run-all: parallel concurrency (default 5; increase for faster run, may stress AI service)
# =============================================================================
DEMO_RUN_ALL_CONCURRENCY

# =============================================================================
# Classifier and action rules (docs/contracts/intent-taxonomy.md, action-set.md)
# =============================================================================
CLASSIFIER_CONFIDENCE_THRESHOLD
AUTO_RESPOND_ENABLED
LLM_MODEL_CLASSIFIER
LLM_MODEL_EXTRACTOR

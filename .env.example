# Agentic Email Processing System — keys only (no secret values)
# See CREATE_SERVICE.md and README.md. Copy to .env and set values.

# =============================================================================
# Service identity
# =============================================================================
NODE_ENV
DOMAIN
SERVICE_NAME
NGINX_NETWORK_NAME

# =============================================================================
# Port configuration
# Allowed range: 33xx (shared microservices). 3367=logging, 3368/3369=notifications,
# 3370=auth. This service: 3371 (blue), 3372 (green).
# =============================================================================
PORT
PORT_BLUE
PORT_GREEN

# =============================================================================
# Shared microservices (same variable names as statex, shop-assistant, notifications)
# =============================================================================
AUTH_SERVICE_URL
LOGGING_SERVICE_URL
LOGGING_SERVICE_API_PATH
AI_SERVICE_URL
NOTIFICATION_SERVICE_URL

# =============================================================================
# Database (optional)
# =============================================================================
DB_HOST
DB_PORT
DB_USER
DB_PASSWORD
DB_NAME

# =============================================================================
# Email ingestion
# =============================================================================
EMAIL_INGESTION_URL
EMAIL_INGESTION_QUEUE

# =============================================================================
# Classifier and action rules (docs/contracts/intent-taxonomy.md, action-set.md)
# =============================================================================
CLASSIFIER_CONFIDENCE_THRESHOLD
AUTO_RESPOND_ENABLED
LLM_MODEL_CLASSIFIER
LLM_MODEL_EXTRACTOR
